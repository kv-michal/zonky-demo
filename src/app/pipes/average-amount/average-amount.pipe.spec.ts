import { AverageAmountPipe } from './average-amount.pipe';

describe('AverageAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
